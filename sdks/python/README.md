# Python SDK

这是第一版 Python 薄 SDK 骨架，目标与 TypeScript SDK 一致：

- 构建 `RunSpec`
- 表达 capability lease
- 不在 SDK 中复制 runtime 语义

当前阶段只做最小 builder，后续再补：

- event stream client
- remote transport client
- child run builder
- golden fixture conformance runner
